---
description: 分析文叙事：不空洞、流畅易懂（板端锚点、术语落地、少旁白）
globs: docs/**/*.md
alwaysApply: false
---

# 文档叙事：流畅、易懂、不空洞

凡**新建或大段改写** `docs/` 下分析 / 调试文，在遵守 `doc-conventions.mdc`（结构）与 `doc-wording.mdc`（用词）之外，按本规则组织叙事。源自 `alsa-hw-params-negotiate` 等改写经验。

## 1. 先走一条街，再发地图

- **先**板端可复现现象（命令 → 日志 / `/proc` / dump），**再**展开机制与源码。
- 开篇若只有 mask/interval/API 表而无数字与报错，读完易「全懂了、全忘了」——视为空洞，须补锚点。

## 2. 一例贯穿，少开平行百科

- 同一篇文章尽量挂在**同一平台、同一条因果链**（成功 + 几类失败对照即可）。
- 手册式 API 罗列、constraint 全家桶进**附录**；正文只保留本路径用到的。

## 3. 术语落地到「在哪」

- 专有名词 / 标志位首次出现时，必须能指到：**具体文件、数组、函数或板端命令**。
- 反例：只写 `KNOT` / 「特殊处理」；正例：写出 `fsl_sai_rates[]` + `constraint_list`，并说明 `rate_min/max` 只是上下界。
- 原则：**抽象结论的同一段里，要有可打开的落点。**

## 4. 现象与机制不一致时，早埋钩子

- 例如 dump 显示 `RATE: [8000 48000]`，实际还有离散 rule：在**第一次出现 dump 处**用半句点破「只是上下界」，并链到后文专节。
- 勿等读者读完全文才发现「区间 ≠ 任意 Hz」。

## 5. 用对照制造记忆

优先写清差异，少堆定义：

- 硬失败 vs `set_rate_near` 告警仍成功；
- 区间外失败 vs 区间内仍失败；
- open 阶段 `No matching …` vs 已 open 后的 refine `-EINVAL`。

## 6. 拆节可以，桥接接故事

- 一节承担「调用树 + 求交 + complete + rule + refine」过密时，拆成多节并加换气。
- 桥接句写**现象 → 下一层在解决什么**，禁止作者旁白式目录预告：
  - 不宜：「先看 A；B 放在下一节」「请勿混淆」。
  - 宜：「前面假定卡上已有合法范围。这张表在 open 里求交写出来。」

## 7. 中文说因果（与用词规范配合）

- 少翻译腔与公文动词：如「登记 / 落下 / 还轮不到 / 生效」（除非已有明确定义）。
- 宜：「挂上 / 写入约束表 / 到不了设参数阶段」。
- 口语能懂但偏碎的词，改成完整因果句（详见 `doc-wording.mdc`）。

## 8. 读者卡壳 = 文债

- 通读或联调时反复追问的点（「限制到底在哪」「dump 为何是范围」），优先补锚点与对照，勿再堆一节纯概念。
- 改完自问：读完能否用一句话复述**本板特有**结论（例如「dump 上下界之外还有 rule_rate + SAI list」）。
