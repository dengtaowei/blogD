---
description: 新增或移动文档时必须同步侧边栏
globs: docs/.vitepress/sidebar.meta.json
alwaysApply: false
---

# 新增 / 移动文档

侧边栏由 **`scripts/generate-sidebar.mjs`** 根据 `docs/.vitepress/sidebar.meta.json` 与各文 frontmatter 自动生成（`docs:dev` / `docs:build` 前会自动跑）。

## 侧栏怎么挂

侧栏里每个子系统一般只留一条入口，链到该目录的 `index.md`；正文不进侧栏。新文章在对应 `index.md` 里加链接即可。`sidebarOrder` / `sidebarTitle` 仍可写，方便以后排序或脚本用。

## 新增文章（已有子系统目录）

1. 在对应目录新建 `.md`（kebab-case）。
2. frontmatter 加 **`sidebarOrder`**（越小越靠前）；可选 **`sidebarTitle`**（短名，默认用 `#` 标题）。长度见 **`doc-conventions.mdc` §8**。
3. 在同目录 **`index.md`** 里补上链接。
4. 跑 `npm run docs:dev` 或 `npm run sync:sidebar` 即可，**不用改 `config.ts`**。

## 新增子系统 / 调整分组

1. 建目录，写好 **`index.md`**（`home: false`，列出文章）。
2. 改 **`docs/.vitepress/sidebar.meta.json`**：`{ "text": "…", "index": "analysis/kernel/…" }`。
3. 再跑 `npm run sync:sidebar`。

## 其他约定

- 正文与 `sidebarTitle` 用词见 **`doc-wording.mdc`**（必须遵守）。
- 正文文章首次 push 后 **`date` 由 `sync:dates` 按 Git 首次提交日写入**；索引页 / 模板设 **`home: false`**（见 `doc-conventions.mdc`）。
- 新建调试笔记可复制 `docs/analysis/kernel/debug/template.md`。
- 站内链接、`nav` 中的 `link` 用**绝对路径、无扩展名**。
