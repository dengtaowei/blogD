---
description: 书写或修改文档时用词严谨、符合通用与规范用语（必须遵守）
globs: docs/**/*.md
alwaysApply: true
---

# 文档用词规范

凡**新建、修订** `docs/` 下 Markdown（含 frontmatter、侧栏标题、索引摘要），必须遵守本规则；与 `doc-conventions.mdc` 的结构约定一并适用。

## 原则

1. **优先规范用语**：采用 USB 规范、Linux 内核文档、子系统常见说法；字段用标准名（如 `bInterfaceClass` / `bInterfaceSubClass` / `bInterfaceProtocol`），正文可写「类 / 子类 / 协议」，避免自造简称（如「类码」）。
2. **名实一致**：表格列名、小节标题与单元格内容一致（列写「类 / 子类 / 协议」就不要只填「描述符」；写「配置」时区分 Configuration 描述符与配置名字符串）。
3. **区分层级**：**设备级**（`bDeviceClass`、VID/PID、整机枚举）与**接口级**（`bInterfaceNumber`、接口 0～4、驱动 bind/probe）分开表述，不混在一句话里下结论。
4. **中文表述完整**：用完整句子说明因果；避免口语缩略（如「起不来」→「未完成 probe / 未出现 `/dev/video0`」）、避免未定义的「正常模式」等带结论色彩的词，除非文中已定义。
5. **正向陈述，少纠错口吻**：写「是什么 / 在哪 / 怎么用」；不要堆「不是…而是…」「请勿混淆」「仅作学习对照」「不随站点部署，请到××打开」一类辩解或元说明。部署约定、版权细则放到 `refs/README` 等专门处，正文只留必要事实与链接。对比两项差异时用对照表或并列句，少用否定句带出正解。
6. **符号与代码**：内核 / 驱动函数、`id_table`、日志原文（如 `invalid HEADER`、`ifdesc raw`）保留英文反引号；中英混排技术文可适当保留 **match**、**probe** 等与源码一致的动词，首次出现可加简短中文说明。
7. **修订时顺改**：改动段落时顺带统一同文内的旧称、编号与交叉引用，避免一篇文档两套术语。

## 示例（宜 / 不宜）

| 不宜 | 宜 |
|------|-----|
| 类码读对 | 类 / 子类 / 协议与 Linux `lsusb` 一致 |
| 1-2:1.1 probe 失败（正文主叙述） | 接口 1 的 `usb_audio_probe` 失败（日志可注 `bus_id`） |
| 表列「描述符」填 `14/1/0` | 表列「类 / 子类 / 协议」或写明字段名 |
| 结果摘要里写「UAC 解析不完整」 | 现象与结论分述；摘要格只写可观测事实（类码、返回值） |
| 完整手册在仓库…（**不随博客站点部署**，请在 GitHub 打开）；厂商原文亦可自行检索 | 完整 PDF 见仓库 `refs/datasheets/WM8960.pdf` |
| 这不是捷径，而是建模补丁，删了一般也能录 | 手册路径是…；驱动用同一控件名画了两条边（见 §7） |

侧栏 `sidebarTitle` 长度见 `doc-conventions.mdc` §8。
