# 手动部署配置模板。复制为 deploy.local.env 后填写，勿提交 deploy.local.env。
# GitHub Actions 自动部署使用仓库 Secrets，不读此文件。
#
#   cp scripts/deploy.example.env scripts/deploy.local.env

REMOTE_USER=root
REMOTE_HOST=your.vps.example
REMOTE_PORT=22
REMOTE_DIR=/var/www/linux-kernel-notes
